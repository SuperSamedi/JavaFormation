spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/decks-a-la-carte_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create